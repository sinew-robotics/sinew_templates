name: Build and deploy gallery

on:
  push:
    branches: [main]
    tags: ["v*"]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: pages-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Install Quarto
        uses: quarto-dev/quarto-actions/setup@v2
        with:
          version: 1.9.38

      - name: Install Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Validate source and contrast
        run: |
          python3 template/scripts/validate.py
          python3 template/scripts/check_contrast.py

      - name: Render and verify every standalone style
        run: template/scripts/render-styles.sh

      - name: Build and verify the zero-config combined gallery
        run: |
          quarto render template --cache-refresh
          template/scripts/check-render.sh template/_site/index.html gallery

      - name: Check that no slide overflows its frame
        run: python3 template/scripts/check_overflow.py template/_site/index.html

      - name: Upload pull-request gallery demo
        if: github.event_name == 'pull_request'
        uses: actions/upload-artifact@v4
        with:
          name: sinew-gallery-pr-${{ github.event.pull_request.number }}
          path: template/_site
          if-no-files-found: error
          retention-days: 14

      - name: Add pull-request demo instructions
        if: github.event_name == 'pull_request'
        run: |
          echo '## Gallery demo' >> "$GITHUB_STEP_SUMMARY"
          echo 'Download the `sinew-gallery-pr-${{ github.event.pull_request.number }}` artifact and open `index.html`.' >> "$GITHUB_STEP_SUMMARY"

      - name: Configure GitHub Pages
        if: github.event_name != 'pull_request'
        uses: actions/configure-pages@v5

      - name: Upload GitHub Pages artifact
        if: github.event_name != 'pull_request'
        uses: actions/upload-pages-artifact@v4
        with:
          path: template/_site

  deploy:
    if: github.event_name != 'pull_request'
    needs: build
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Deploy GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
